# Project Instructions

## Code Review

When the user says "audit my code", "code audit", or "review my code", always use the `superpowers:code-reviewer` agent (via the Agent tool with `subagent_type: "superpowers:code-reviewer"`) instead of performing a manual review.
